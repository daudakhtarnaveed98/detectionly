// Import modules.
import React from "react";
import Authentication from "../Authentication";

// Detectionly component.
function Detectionly() {
  return (
    <div>
      <Authentication />
    </div>
  );
}

// Export.
export default Detectionly;
